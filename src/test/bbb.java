package test;

import java.util.Scanner;

public class bbb {
    public static void main(String[] args) {

        System.out.println(0.1 + 0.2 == 0.3);

        /* 
        Scanner in = new Scanner(System.in);
        System.out.print("Would you like to see an X or O? ");
        String ch = in.next();
        System.out.print("What size should it be? ");
        int size = in.nextInt();

        if(ch.equalsIgnoreCase("x")) {
            for (int i = 0; i < size; i++) {
                for (int j = 0; j < size; j++) {

                    // check if on diagonal from both sides
                    if (i == j || i == size - 1 - j) {
                        System.out.print("*");
                    } else {
                        System.out.print(" ");
                    }
                }
                System.out.println();
            }

        } else {
            for (int i = 0; i < size; i++) {
                for (int j = 0; j < size; j++) {

                    // check if top/bottom, and if the first/last column
                    if (i == 0 || i == size - 1 || j == 0 || j == size - 1) {
                        System.out.print("*");
                    } else {
                        System.out.print(" ");
                    }
                }
                System.out.println();
            }
        }
        */
    }
}
